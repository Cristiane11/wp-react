strict mode(strict operate contest)
how to enable it write( how to turn the switch on)
Type "use strict"( it is a string; so use the cotation symbol)
//Not strict mode..
function newCode(){
	"use strict";
//Strict Mode...
}
